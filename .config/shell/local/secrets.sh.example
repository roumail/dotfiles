#!/usr/bin/env bash
# Local secrets (NOT in git)
# Copy to secrets.sh and add sensitive credentials

# Never commit this file to version control
# Use for storing API keys, tokens, credentials, etc.

# Example:
# export AWS_ACCESS_KEY_ID="..."
# export API_TOKEN="..."
